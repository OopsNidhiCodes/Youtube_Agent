# Free alternatives for YouTube video agent
# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
loguru>=0.7.2
schedule>=1.2.0

# Video processing (free)
moviepy==1.0.3
imageio>=2.5,<3.0
decorator>=4.0.2,<5.0
proglog<=1.0.0
opencv-python>=4.8.0
imageio-ffmpeg>=0.4.9

# Audio processing (free)
gTTS>=2.4.0

# Web scraping (free)
beautifulsoup4>=4.12.0
feedparser>=6.0.10

# Free AI alternatives
huggingface-hub>=0.17.0
transformers>=4.35.0
# NOTE: Install CPU-only PyTorch separately to avoid CUDA downloads.
# On Linux/Render, the build script installs torch via CPU index-url.
# On Windows locally, install with:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.2.2

# Image processing (free)
Pillow>=10.0.0
numpy>=1.24.0

# Optional: For local Stable Diffusion (if available)
# diffusers>=0.21.0
# accelerate>=0.24.0